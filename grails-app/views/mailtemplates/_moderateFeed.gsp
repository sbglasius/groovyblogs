<p>
    Request to approve URL: ${blog.title} at url <a href="${blog.feedUrl}">${blog.feedUrl}</a>
</p>
<p>
    <a href="${approve}">Approve feed</a> for ${blog.account.email} or <a href="${reject}">Remove feed</a>.
</p>
